on_actions = {
	on_daily = {
		effect = {
			if = {
				limit = {
					tag = SOV
					date > 2029.1.5
					#NOT = { has_war = yes }
					NOT = { has_country_flag = rev_sov_baltic_war_fired }
					OR = {
						696 = { is_controlled_by = SOV }
						NOT = { is_ai = yes }
					}
					OR = {
						USA = { has_war_with = CAN }
						USA = { has_war_with = GER }
						USA = { has_war_with = POL }
					}
				}
				country_event = { id = rev_sov_baltic.1 }
			}
		}
	}
}
