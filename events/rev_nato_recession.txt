add_namespace = rev_nato

country_event = {
	id = rev_nato.1
	title = rev_nato.1.t
	desc = rev_nato.1.d
	picture = GFX_report_event_generic_economic
	is_triggered_only = yes

	trigger = {
		OR = {
			tag = BEL
			tag = CAN
			tag = ENG
			tag = FRA
			tag = GER
			tag = HOL
			tag = ITA
			tag = SPR
		}
		NOT = { has_country_flag = REV_NATO_2026_recession }
		date > 2026.5.1
	}

	option = {
		name = rev_nato.1.a
		set_country_flag = REV_NATO_2026_recession
		if = {
			limit = { has_idea = stable_growth }
			remove_ideas = stable_growth
		}
		if = {
			limit = { NOT = { has_idea = stagnation } }
			add_ideas = stagnation
		}
		add_stability = -0.1
	}
}
